这是一个简单的聊天室程序

开发语言：C语言
运行环境：Linux

使用方法：
先执行make，生成server和client两个可执行文件
开启服务器程序./server
开启客户端程序,并指定服务器IP地址 ./client 127.0.0.1 
可以开启多个客户端形成一个聊天室。
当有新成员加入时，服务器会向所有在线用户发出提示；当有人退出时服务器也会通知所有人。
服务器还可以显示在线人数。

R.Wang
2012.1.19

